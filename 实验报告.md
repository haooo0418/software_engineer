# 个人记账本系统实验报告

## 一、项目概述

本次实验实现了一个功能完整的C++个人记账本系统，支持用户管理、交易记录、统计分析和数据可视化等功能。系统采用命令行界面，数据持久化存储，代码规模约1775行。

## 二、UML图到代码的实现

### 2.1 设计思路

系统采用面向对象设计，将功能模块化为多个类：

- **User类**：负责用户信息管理和身份验证
- **Transaction类**：管理交易记录（收入/支出）
- **AccountingSystem类**：系统核心控制器，协调各模块
- **Statistics类**：提供统计分析功能
- **Visualization类**：实现数据可视化（柱状图、饼图）

### 2.2 代码实现规模

- **源文件**：11个文件（5个头文件 + 6个源文件）
- **代码总量**：约1775行
- **主要模块**：
  - User模块：约150行
  - Transaction模块：约180行
  - AccountingSystem模块：约400行
  - Statistics模块：约120行
  - Visualization模块：约280行
  - main主程序：约600行

### 2.3 UML到代码的转换过程

从UML类图到实际代码，我遵循以下步骤：

1. **类的定义**：根据UML类图定义类的属性和方法
2. **数据封装**：使用private成员变量，提供public的getter/setter方法
3. **关系实现**：
   - 组合关系：AccountingSystem包含User和Transaction的集合
   - 依赖关系：通过方法参数传递对象引用
4. **功能实现**：按照UML方法列表逐一实现业务逻辑

例如，User类的实现：

```cpp
class User {
private:
    std::string username;
    std::string password;
    std::string userId;

public:
    User(std::string username, std::string password);
    bool authenticate(const std::string& password);
    // 其他方法...
};
```

## 三、大模型辅助开发过程

### 3.1 大模型的主要作用

在本次实验中，我使用GitHub Copilot辅助完成了以下工作：

#### 1. 代码框架搭建
- 生成CMakeLists.txt配置文件
- 创建项目目录结构
- 定义类的基本框架和接口

#### 2. 功能实现
- **数据持久化**：实现文件读写的序列化/反序列化逻辑
- **统计分析**：按时间、分类、账户维度统计数据
- **数据可视化**：用ASCII字符绘制柱状图和饼图

#### 3. 问题修复
- **编译错误修复**：解决Windows MSVC的字符编码问题
- **跨平台兼容**：处理不同操作系统的API差异（如mkdir函数）
- **字符显示问题**：将Unicode字符替换为ASCII避免乱码

#### 4. 代码优化
- 改进图表显示效果，添加边框、标题、刻度
- 实现饼图和柱状图的差异化设计
- 优化数据格式和对齐方式

### 3.2 具体工作实例

**示例1：数据可视化功能**

我向大模型描述需求："实现一个ASCII柱状图，显示月度收支统计"，大模型生成了基础代码框架。然后我进一步要求"添加刻度参考和数据标签"，大模型完善了细节。

**示例2：编译错误修复**

遇到Windows编译错误时，我将错误日志提供给大模型，它准确识别出：
- C4819：字符编码问题
- C3861：缺少Windows特定头文件
- C4458：参数名称遮蔽问题

并提供了相应的解决方案。

### 3.3 处理不符合预期的情况

当大模型生成的内容不符合预期时，我采取以下策略：

#### 1. 明确需求
最初的饼图显示效果与柱状图太相似，用户无法区分。我明确提出："饼图需要有明显不同的视觉效果，使用不同的ASCII字符表示"。大模型重新设计了饼图，添加了ASCII艺术风格的图标和图例。

#### 2. 迭代改进
对于图表显示功能，我分多次迭代：
- 第1次：基础柱状图
- 第2次：添加边框和标题
- 第3次：改进数据格式和对齐
- 第4次：添加汇总信息
- 第5次：重新设计饼图使其更具区分度

#### 3. 代码审查
每次大模型生成代码后，我都会：
- 检查逻辑正确性
- 测试边界条件
- 验证跨平台兼容性
- 确认符合需求规格

#### 4. 手动调整
对于一些细节问题，我会：
- 调整参数值（如图表宽度、分类数量）
- 修改字符串格式化方式
- 优化算法性能

## 四、开发过程总结

### 4.1 开发流程

1. **需求分析** → 明确系统功能和模块划分
2. **设计阶段** → 使用UML建模，定义类结构
3. **编码实现** → 借助大模型快速生成代码框架
4. **测试调试** → 发现问题，利用大模型辅助修复
5. **优化完善** → 改进用户体验和代码质量

### 4.2 大模型使用心得

**优势：**
- 提高开发效率，快速生成样板代码
- 帮助解决编译错误和技术难题
- 提供多种实现方案供选择

**局限性：**
- 生成的代码需要人工审查和调整
- 对于复杂业务逻辑需要明确指导
- 需要多轮迭代才能达到理想效果

**最佳实践：**
- 清晰描述需求和预期效果
- 分步骤、分模块地请求帮助
- 对生成的代码进行测试和验证
- 遇到问题及时反馈，引导大模型改进

### 4.3 技术难点突破

1. **跨平台兼容性**
   - 问题：Windows和Linux的API差异
   - 解决：使用条件编译和平台相关的头文件

2. **字符编码问题**
   - 问题：中文和特殊符号在Windows下乱码
   - 解决：全部使用ASCII字符，确保跨平台一致性

3. **数据可视化**
   - 问题：纯文本实现图表具有挑战性
   - 解决：创新使用ASCII字符组合，设计差异化的图表样式

## 五、项目成果

### 5.1 核心功能

- ✅ 用户注册和登录
- ✅ 记账管理（增删改查）
- ✅ 多维度统计（时间、分类、账户）
- ✅ 数据可视化（柱状图、饼图）
- ✅ 关键词搜索
- ✅ 数据持久化

### 5.2 技术特点

- 面向对象设计，模块化清晰
- 跨平台兼容（Windows/Linux）
- ASCII字符可视化，无乱码问题
- 代码规范，注释完整

### 5.3 项目文档

- README.md：完整使用说明
- EXAMPLES.md：使用示例
- SCREENSHOTS.md：界面展示
- PROJECT_SUMMARY.md：项目总结

## 六、总结与反思

通过本次实验，我深刻体会到：

1. **系统设计的重要性**：良好的UML设计能显著提升开发效率
2. **大模型的价值**：合理利用AI工具可以事半功倍
3. **迭代开发的必要性**：软件开发是一个持续改进的过程
4. **问题解决能力**：遇到困难时要善于分析和寻求帮助

大模型是一个强大的辅助工具，但不能完全依赖。开发者需要具备：
- 清晰的需求理解能力
- 代码审查和调试能力
- 与AI有效沟通的能力
- 持续学习和改进的意识

本次实验让我对软件工程实践有了更深入的理解，也提升了我使用现代AI工具进行开发的能力。
